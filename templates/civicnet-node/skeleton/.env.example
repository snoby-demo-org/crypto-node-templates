# Docker image to run (built by build.sh, or prebuilt). See README.
IMAGE_REPO=iotapi322/civicnet
# Image tag = local source commit SHA by default; override here.
IMAGE_TAG=latest
# Host ports
RPC_PORT=9332
P2P_PORT=9333
